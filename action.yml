name: 'Airdb Actions'
description: 'Get started with Container actions'
author: 'GitHub'
branding:
  color: 'yellow'
  icon: 'bell'

inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}
