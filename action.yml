name: 'Airdb Actions'
description: 'Airdb Container Actions'
author: 'deancn'

branding:
  color: 'yellow'
  icon: 'bell'

inputs: 
  myCommand:
    description: 'airdb github command'
    default: 'issue'
runs:
  using: 'docker'
  image: 'docker://airdb/ubuntu'
  env:
    GITHUB_TOKEN: $GITHUB_TOKEN
  args:
    - ${{ inputs.myCommand }}
