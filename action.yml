name: 'Airdb Actions'
description: 'Get started with Container actions'
author: 'GitHub'
branding:
  icon: 'login.svg'
  color: 'blue'

inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}
